{% extends "common/layout/base.njk" %}

{% from "govuk/components/date-input/macro.njk" import govukDateInput %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% set pageTitleName = 'general.selectPhotoId.pageTitle' | translate %}
{% set hrefBack = back %} 
{% set showBack = true %}
{% set showError = isInvalid %}

{% block content %}

<h1 class="govuk-heading-l"> {{ "general.brpExpiry.header" | translate }} </h1>

<form id="form-tracking" action="/cic/oauth2/brpDetails" method="post">
<p>{{ "general.brpExpiry.body" | translate }}</p>

{{ govukDateInput({
  id: "brp-expiry",
  namePrefix: "brp-expiry",
  fieldset: {
    legend: {
      text: "general.brpExpiry.dateOfExpiry" | translate
    }
  },
  hint: {
    text: "general.brpExpiry.example" | translate
  }
}) }}


{{ govukButton({
  text: "general.buttons.continue" | translate,
  type: "submit"
}) }}

</form>

{% endblock %}